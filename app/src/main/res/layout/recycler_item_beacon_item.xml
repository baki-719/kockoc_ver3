<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="132dp">

        <ImageView
            android:id="@+id/beacon_point_image"
            android:layout_width="160dp"
            android:layout_height="120dp"
            android:layout_alignParentStart="true"
            android:layout_alignTop="@+id/beacon_point_name"
            android:scaleType="fitXY"
            app:srcCompat="@drawable/app_icon" />

        <TextView
            android:id="@+id/beacon_point_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="165dp"
            android:layout_marginTop="5dp"
            android:text="경복궁"
            android:textColor="#f7887b"
            android:textSize="19dp" />

        <TextView
            android:id="@+id/beacon_point_overview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignStart="@+id/beacon_point_name"
            android:layout_below="@+id/beacon_point_name"
            android:text="경복궁은 6월....." />
    </RelativeLayout>
</LinearLayout>